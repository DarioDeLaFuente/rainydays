## RUN DOCKER

`docker-compose up -d`

`docker-compose start`

`npm install`

## END DOCKER
> `docker-compose stop`


## RUN SASS

`npm run bulid`

`npm run watch`


> ## Rebuild
>`npm rebuild node-sass`